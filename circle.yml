machine:
  environment:
    GRADLE_OPTS: -Xmx512m

general:
  build_dir: Mentions

test:
  override:
   - gradlew test --stacktrace
  post:
    - pip install --user codecov 
    - codecov --token=$CODECOV_TOKEN
