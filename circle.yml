machine:
  environment:
    ANDROID_HOME: /home/ubuntu/cache/android-sdk-linux
    GRADLE_OPTS: -Xmx512m

test:
  override:
  - ./Mentions/gradlew test --stacktrace
  post:
    - pip install --user codecov 
    - codecov --token=$CODECOV_TOKEN
